server:
  port: 8080

spring:
  datasource:
    main:
      jdbc-url: jdbc:postgresql://localhost:5432/main
      username: postgres
      password: password
    first:
      jdbc-url: jdbc:postgresql://localhost:5432/first
      username: postgres
      password: password
    second:
      jdbc-url: jdbc:postgresql://localhost:5432/second
      username: postgres
      password: password
    third:
      jdbc-url: jdbc:sqlserver://localhost:1433;databaseName=third;integratedSecurity=false;encrypt=false;
      username: sa
      password: sa
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none